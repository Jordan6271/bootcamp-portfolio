import React, { Fragment } from "react";

const Progression = (props) => {
    return (
        <Fragment>
            <h2>Progression</h2>
            <p>{props.text}</p>
        </Fragment>
    );
};

export default Progression;
